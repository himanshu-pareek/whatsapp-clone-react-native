import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        padding: 10,
    },

    messageBox: {
        borderRadius: 5,
        padding: 10,
    },

    message: {

    },

    name: {
        fontWeight: 'bold',
    },

    time: {
        alignSelf: 'flex-end',
        color: 'grey',
    }
});

export default styles;
